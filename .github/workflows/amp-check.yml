name: AMP Status Check

on:
  schedule:
    - cron: '*/30 * * * *'  # Runs every 30 mins
  workflow_dispatch:       # Allows manual trigger

jobs:
  check-server:
    runs-on: ubuntu-latest
    steps:
      - name: Connect to Tailscale
        uses: tailscale/github-action@v2
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:github  # Ensure this tag matches what you set in Tailscale

      - name: Check AMP Web Service
        run: |
          # -f: Fail on error
          # -s: Silent (cleaner logs)
          # -S: Show error if it fails
          # -v: Verbose (to see the handshake)
          curl -fSv --connect-timeout 10 http://[IP_ADDRESS] || exit 1
